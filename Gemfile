# frozen_string_literal: true

source "https://rubygems.org"

# gem "rails"

gem 'discordrb'
gem 'faye-websocket'   # WebSocket client
gem 'eventmachine'     # event loop (faye needs it)
gem 'bigdecimal'
gem 'dotenv'
