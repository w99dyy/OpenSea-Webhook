# Add, edit or exclude NFT Collection

To add or edit NFT collection go to `config.rb` and edit this `COLLECTIONS = ['mash-it']`

And to exclude and NFT just add NFT name here:
`  EXCLUDED_NFTS = [
    'Jay and pets'
  ].map(&:downcase)
`
